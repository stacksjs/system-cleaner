dependencies:
  # 0.0.83 carries the menu fix (a role item with no id was skipped), but the
  # pantry registry has not indexed 0.0.82 or 0.0.83 — both answer "not found in
  # registry" while the GitHub release has every asset. Nothing here needs it:
  # @stacksjs/desktop sends an id on every item since 0.2.249, so the native
  # fallback is defence in depth. Bump when the registry catches up.
  craft-native.org: 0.0.81
  bun.sh: ^1.3.14
