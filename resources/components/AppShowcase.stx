<script server>
export const lead = {
  name: 'Dashboard',
  copy: 'A health score, the four numbers that move it, and a live CPU and memory plot with labelled axes on both sides. Enough to know whether anything needs you today.',
  image: 'dashboard',
  href: '/app',
}

export const pair = [
  {
    name: 'Live Processes',
    copy: 'CPU and memory pressure in context. Filter the list without losing the system-wide picture.',
    image: 'processes',
    href: '/app/processes',
  },
  {
    name: 'Quick Clean',
    copy: 'Safe categories, every target visible, and nothing removed until you say so.',
    image: 'cleanup',
    href: '/app/cleanup',
  },
]
</script>

<section id="app-tour" class="app-tour section" aria-labelledby="app-tour-title">
  <div class="site-container">
    <div class="section-heading">
      <h2 id="app-tour-title">One source list. Three clear jobs.</h2>
      <p class="section-intro">Every view keeps the same navigation, the same density, and the same local-first posture.</p>
    </div>

    <div class="tour-lead">
      <figure class="app-window">
        <figcaption class="app-window-bar">
          <span class="traffic-lights" aria-hidden="true"><i></i><i></i><i></i></span>
          <span>{{ lead.name }}</span>
        </figcaption>
        <picture>
          <source media="(prefers-color-scheme: dark)" srcset="/images/showcase/{{ lead.image }}-dark.png" />
          <img src="/images/showcase/{{ lead.image }}-light.png" width="1400" height="900" loading="lazy" alt="SystemCleaner {{ lead.name }} view" />
        </picture>
      </figure>
      <div class="tour-lead-copy">
        <h3>{{ lead.name }}</h3>
        <div>
          <p>{{ lead.copy }}</p>
          <a class="tour-link" href="{{ lead.href }}" data-stx-link>Open {{ lead.name }} <span class="i-f7-arrow-right" aria-hidden="true"></span></a>
        </div>
      </div>
    </div>

    <div class="tour-pair">
      @foreach (pair as view)
        <article class="tour-card">
          <figure class="app-window">
            <figcaption class="app-window-bar">
              <span class="traffic-lights" aria-hidden="true"><i></i><i></i><i></i></span>
              <span>{{ view.name }}</span>
            </figcaption>
            <picture>
              <source media="(prefers-color-scheme: dark)" srcset="/images/showcase/{{ view.image }}-dark.png" />
              <img src="/images/showcase/{{ view.image }}-light.png" width="1400" height="900" loading="lazy" alt="SystemCleaner {{ view.name }} view" />
            </picture>
          </figure>
          <div>
            <h3>{{ view.name }}</h3>
            <p>{{ view.copy }}</p>
            <a class="tour-link" href="{{ view.href }}" data-stx-link>Open {{ view.name }} <span class="i-f7-arrow-right" aria-hidden="true"></span></a>
          </div>
        </article>
      @endforeach
    </div>
  </div>
</section>
