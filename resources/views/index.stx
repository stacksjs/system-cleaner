<script server>
import { FEATURES } from '~/app/Support/Marketing/catalog'

const features = FEATURES
const seed = 'SystemCleaner'.length
const heroLayouts = ['cinematic-center', 'editorial-showcase', 'artistic-asymmetry']
const typographyStacks = ['outfit', 'satoshi', 'cabinet-grotesk', 'geist']
const selectedHeroLayout = heroLayouts[seed % heroLayouts.length]
const selectedTypography = typographyStacks[(seed * 7) % typographyStacks.length]
</script>

@extends('layouts/marketing')

@section('title', 'SystemCleaner - A clear view of your Mac')
@section('description', 'Understand disk usage, clean safe files, audit startup items, and monitor system health with a private native macOS app.')
@section('canonical', 'https://system-cleaner.app/')

@section('content')
  <div class="type-{{ selectedTypography }}">
    <MarketingHero :layout="selectedHeroLayout" />
    <FeatureBento :features="features" />
    <AppShowcase />
  </div>

  <section id="privacy" class="section site-container">
    <div class="privacy-band">
      <div>
        <span class="privacy-icon i-f7-lock-shield-fill" aria-hidden="true"></span>
        <h2>Your Mac stays your business.</h2>
      </div>
      <p>Scans, health readings, process data, and cleanup decisions stay local. SystemCleaner has no account requirement, cloud scan, or analytics pipeline.</p>
    </div>
  </section>
@endsection
